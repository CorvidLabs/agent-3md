# CLAUDE.md

This repo is governed by the CorvidLabs trust toolchain. See AGENTS.md for the
standing rules block and follow it before making changes (run `fledge lanes run
verify`; never bypass the augur, attest, or spec gates).
