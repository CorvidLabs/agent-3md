---
3md: 1.0
axis: skill
title: Atlas-10
agent: atlas
model: claude-opus-4-8
version: "1.0"
tools: web, bash, fs
entry: "0"
note: synthetic agent with 10 skills, for the scaling benchmark.
---
A research-and-build agent packaged as one 3md document with 10 skills. The
runtime loads only the skill plane a request needs.

@plane z=0 label="Atlas" kind=identity
# Atlas

A careful agent. Route each task to the skill whose triggers match, load that
plane only, and follow its dependency links. Confirm destructive actions.

@plane z=1 label="cite-sources" kind=skill triggers="cite, source, reference, attribution" inputs="claims"
# Skill: cite-sources

Attach a verifiable source to every external claim; mark anything unverified.

@plane z=2 label="search-record" kind=skill triggers="search, record, search record, batch" inputs="record"
# Skill: search-record

Search the record. Steps:

1. Validate the incoming record and its parameters.
2. Search it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=3 label="summarize-doc" kind=skill triggers="summarize, doc, summarize doc, now" inputs="doc"
# Skill: summarize-doc

Summarize the doc. Steps:

1. Validate the incoming doc and its parameters.
2. Summarize it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=4 label="translate-table" kind=skill triggers="translate, table, translate table, this" inputs="table"
# Skill: translate-table

Translate the table. Steps:

1. Validate the incoming table and its parameters.
2. Translate it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=5 label="classify-event" kind=skill triggers="classify, event, classify event, this" inputs="event"
# Skill: classify-event

Classify the event. Steps:

1. Validate the incoming event and its parameters.
2. Classify it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=6 label="extract-report" kind=skill triggers="extract, report, extract report, this" inputs="report"
# Skill: extract-report

Extract the report. Steps:

1. Validate the incoming report and its parameters.
2. Extract it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=7 label="render-table" kind=skill triggers="render, table, render table, auto" inputs="table"
# Skill: render-table

Render the table. Steps:

1. Validate the incoming table and its parameters.
2. Render it using the appropriate backend/tool.
3. Return a structured result the caller can act on.
- cross-check results against [[z=1|cite-sources]].

@plane z=8 label="compile-asset" kind=skill triggers="compile, asset, compile asset, auto" inputs="asset"
# Skill: compile-asset

Compile the asset. Steps:

1. Validate the incoming asset and its parameters.
2. Compile it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=9 label="deploy-model" kind=skill triggers="deploy, model, deploy model, this" inputs="model"
# Skill: deploy-model

Deploy the model. Steps:

1. Validate the incoming model and its parameters.
2. Deploy it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=10 label="encrypt-file" kind=skill triggers="encrypt, file, encrypt file, this" inputs="file"
# Skill: encrypt-file

Encrypt the file. Steps:

1. Validate the incoming file and its parameters.
2. Encrypt it using the appropriate backend/tool.
3. Return a structured result the caller can act on.
- cross-check results against [[z=1|cite-sources]].

