---
3md: 1.0
axis: skill
title: Atlas-50
agent: atlas
model: claude-opus-4-8
version: "1.0"
tools: web, bash, fs
entry: "0"
note: synthetic agent with 50 skills, for the scaling benchmark.
---
A research-and-build agent packaged as one 3md document with 50 skills. The
runtime loads only the skill plane a request needs.

@plane z=0 label="Atlas" kind=identity
# Atlas

A careful agent. Route each task to the skill whose triggers match, load that
plane only, and follow its dependency links. Confirm destructive actions.

@plane z=1 label="cite-sources" kind=skill triggers="cite, source, reference, attribution" inputs="claims"
# Skill: cite-sources

Attach a verifiable source to every external claim; mark anything unverified.

@plane z=2 label="search-record" kind=skill triggers="search, record, search record, auto" inputs="record"
# Skill: search-record

Search the record. Steps:

1. Validate the incoming record and its parameters.
2. Search it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=3 label="summarize-file" kind=skill triggers="summarize, file, summarize file, auto" inputs="file"
# Skill: summarize-file

Summarize the file. Steps:

1. Validate the incoming file and its parameters.
2. Summarize it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=4 label="translate-ticket" kind=skill triggers="translate, ticket, translate ticket, batch" inputs="ticket"
# Skill: translate-ticket

Translate the ticket. Steps:

1. Validate the incoming ticket and its parameters.
2. Translate it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=5 label="classify-asset" kind=skill triggers="classify, asset, classify asset, please" inputs="asset"
# Skill: classify-asset

Classify the asset. Steps:

1. Validate the incoming asset and its parameters.
2. Classify it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=6 label="extract-stream" kind=skill triggers="extract, stream, extract stream, please" inputs="stream"
# Skill: extract-stream

Extract the stream. Steps:

1. Validate the incoming stream and its parameters.
2. Extract it using the appropriate backend/tool.
3. Return a structured result the caller can act on.
- cross-check results against [[z=1|cite-sources]].

@plane z=7 label="render-report" kind=skill triggers="render, report, render report, auto" inputs="report"
# Skill: render-report

Render the report. Steps:

1. Validate the incoming report and its parameters.
2. Render it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=8 label="compile-model" kind=skill triggers="compile, model, compile model, this" inputs="model"
# Skill: compile-model

Compile the model. Steps:

1. Validate the incoming model and its parameters.
2. Compile it using the appropriate backend/tool.
3. Return a structured result the caller can act on.
- cross-check results against [[z=1|cite-sources]].

@plane z=9 label="deploy-image" kind=skill triggers="deploy, image, deploy image, now" inputs="image"
# Skill: deploy-image

Deploy the image. Steps:

1. Validate the incoming image and its parameters.
2. Deploy it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=10 label="encrypt-model" kind=skill triggers="encrypt, model, encrypt model, batch" inputs="model"
# Skill: encrypt-model

Encrypt the model. Steps:

1. Validate the incoming model and its parameters.
2. Encrypt it using the appropriate backend/tool.
3. Return a structured result the caller can act on.
- cross-check results against [[z=1|cite-sources]].

@plane z=11 label="compress-request" kind=skill triggers="compress, request, compress request, batch" inputs="request"
# Skill: compress-request

Compress the request. Steps:

1. Validate the incoming request and its parameters.
2. Compress it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=12 label="schedule-report" kind=skill triggers="schedule, report, schedule report, this" inputs="report"
# Skill: schedule-report

Schedule the report. Steps:

1. Validate the incoming report and its parameters.
2. Schedule it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=13 label="notify-request" kind=skill triggers="notify, request, notify request, please" inputs="request"
# Skill: notify-request

Notify the request. Steps:

1. Validate the incoming request and its parameters.
2. Notify it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=14 label="validate-report" kind=skill triggers="validate, report, validate report, auto" inputs="report"
# Skill: validate-report

Validate the report. Steps:

1. Validate the incoming report and its parameters.
2. Validate it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=15 label="format-data" kind=skill triggers="format, data, format data, auto" inputs="data"
# Skill: format-data

Format the data. Steps:

1. Validate the incoming data and its parameters.
2. Format it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=16 label="parse-file" kind=skill triggers="parse, file, parse file, now" inputs="file"
# Skill: parse-file

Parse the file. Steps:

1. Validate the incoming file and its parameters.
2. Parse it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=17 label="scrape-file" kind=skill triggers="scrape, file, scrape file, auto" inputs="file"
# Skill: scrape-file

Scrape the file. Steps:

1. Validate the incoming file and its parameters.
2. Scrape it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=18 label="crawl-query" kind=skill triggers="crawl, query, crawl query, now" inputs="query"
# Skill: crawl-query

Crawl the query. Steps:

1. Validate the incoming query and its parameters.
2. Crawl it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=19 label="index-record" kind=skill triggers="index, record, index record, please" inputs="record"
# Skill: index-record

Index the record. Steps:

1. Validate the incoming record and its parameters.
2. Index it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=20 label="embed-ticket" kind=skill triggers="embed, ticket, embed ticket, please" inputs="ticket"
# Skill: embed-ticket

Embed the ticket. Steps:

1. Validate the incoming ticket and its parameters.
2. Embed it using the appropriate backend/tool.
3. Return a structured result the caller can act on.
- cross-check results against [[z=1|cite-sources]].

@plane z=21 label="cluster-report" kind=skill triggers="cluster, report, cluster report, now" inputs="report"
# Skill: cluster-report

Cluster the report. Steps:

1. Validate the incoming report and its parameters.
2. Cluster it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=22 label="rank-query" kind=skill triggers="rank, query, rank query, this" inputs="query"
# Skill: rank-query

Rank the query. Steps:

1. Validate the incoming query and its parameters.
2. Rank it using the appropriate backend/tool.
3. Return a structured result the caller can act on.
- cross-check results against [[z=1|cite-sources]].

@plane z=23 label="dedupe-table" kind=skill triggers="dedupe, table, dedupe table, this" inputs="table"
# Skill: dedupe-table

Dedupe the table. Steps:

1. Validate the incoming table and its parameters.
2. Dedupe it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=24 label="merge-file" kind=skill triggers="merge, file, merge file, batch" inputs="file"
# Skill: merge-file

Merge the file. Steps:

1. Validate the incoming file and its parameters.
2. Merge it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=25 label="split-model" kind=skill triggers="split, model, split model, batch" inputs="model"
# Skill: split-model

Split the model. Steps:

1. Validate the incoming model and its parameters.
2. Split it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=26 label="convert-table" kind=skill triggers="convert, table, convert table, please" inputs="table"
# Skill: convert-table

Convert the table. Steps:

1. Validate the incoming table and its parameters.
2. Convert it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=27 label="resize-data" kind=skill triggers="resize, data, resize data, auto" inputs="data"
# Skill: resize-data

Resize the data. Steps:

1. Validate the incoming data and its parameters.
2. Resize it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=28 label="crop-doc" kind=skill triggers="crop, doc, crop doc, now" inputs="doc"
# Skill: crop-doc

Crop the doc. Steps:

1. Validate the incoming doc and its parameters.
2. Crop it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=29 label="annotate-table" kind=skill triggers="annotate, table, annotate table, batch" inputs="table"
# Skill: annotate-table

Annotate the table. Steps:

1. Validate the incoming table and its parameters.
2. Annotate it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=30 label="transcribe-asset" kind=skill triggers="transcribe, asset, transcribe asset, this" inputs="asset"
# Skill: transcribe-asset

Transcribe the asset. Steps:

1. Validate the incoming asset and its parameters.
2. Transcribe it using the appropriate backend/tool.
3. Return a structured result the caller can act on.
- cross-check results against [[z=1|cite-sources]].

@plane z=31 label="caption-report" kind=skill triggers="caption, report, caption report, batch" inputs="report"
# Skill: caption-report

Caption the report. Steps:

1. Validate the incoming report and its parameters.
2. Caption it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=32 label="diff-file" kind=skill triggers="diff, file, diff file, batch" inputs="file"
# Skill: diff-file

Diff the file. Steps:

1. Validate the incoming file and its parameters.
2. Diff it using the appropriate backend/tool.
3. Return a structured result the caller can act on.
- cross-check results against [[z=1|cite-sources]].

@plane z=33 label="patch-model" kind=skill triggers="patch, model, patch model, auto" inputs="model"
# Skill: patch-model

Patch the model. Steps:

1. Validate the incoming model and its parameters.
2. Patch it using the appropriate backend/tool.
3. Return a structured result the caller can act on.
- cross-check results against [[z=1|cite-sources]].

@plane z=34 label="lint-stream" kind=skill triggers="lint, stream, lint stream, now" inputs="stream"
# Skill: lint-stream

Lint the stream. Steps:

1. Validate the incoming stream and its parameters.
2. Lint it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=35 label="refactor-data" kind=skill triggers="refactor, data, refactor data, now" inputs="data"
# Skill: refactor-data

Refactor the data. Steps:

1. Validate the incoming data and its parameters.
2. Refactor it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=36 label="profile-report" kind=skill triggers="profile, report, profile report, please" inputs="report"
# Skill: profile-report

Profile the report. Steps:

1. Validate the incoming report and its parameters.
2. Profile it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=37 label="benchmark-file" kind=skill triggers="benchmark, file, benchmark file, auto" inputs="file"
# Skill: benchmark-file

Benchmark the file. Steps:

1. Validate the incoming file and its parameters.
2. Benchmark it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=38 label="mock-doc" kind=skill triggers="mock, doc, mock doc, this" inputs="doc"
# Skill: mock-doc

Mock the doc. Steps:

1. Validate the incoming doc and its parameters.
2. Mock it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=39 label="seed-stream" kind=skill triggers="seed, stream, seed stream, this" inputs="stream"
# Skill: seed-stream

Seed the stream. Steps:

1. Validate the incoming stream and its parameters.
2. Seed it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=40 label="migrate-file" kind=skill triggers="migrate, file, migrate file, auto" inputs="file"
# Skill: migrate-file

Migrate the file. Steps:

1. Validate the incoming file and its parameters.
2. Migrate it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=41 label="backup-asset" kind=skill triggers="backup, asset, backup asset, this" inputs="asset"
# Skill: backup-asset

Backup the asset. Steps:

1. Validate the incoming asset and its parameters.
2. Backup it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=42 label="restore-table" kind=skill triggers="restore, table, restore table, this" inputs="table"
# Skill: restore-table

Restore the table. Steps:

1. Validate the incoming table and its parameters.
2. Restore it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=43 label="sync-record" kind=skill triggers="sync, record, sync record, batch" inputs="record"
# Skill: sync-record

Sync the record. Steps:

1. Validate the incoming record and its parameters.
2. Sync it using the appropriate backend/tool.
3. Return a structured result the caller can act on.
- cross-check results against [[z=1|cite-sources]].

@plane z=44 label="upload-record" kind=skill triggers="upload, record, upload record, batch" inputs="record"
# Skill: upload-record

Upload the record. Steps:

1. Validate the incoming record and its parameters.
2. Upload it using the appropriate backend/tool.
3. Return a structured result the caller can act on.
- cross-check results against [[z=1|cite-sources]].

@plane z=45 label="download-record" kind=skill triggers="download, record, download record, auto" inputs="record"
# Skill: download-record

Download the record. Steps:

1. Validate the incoming record and its parameters.
2. Download it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=46 label="stream-asset" kind=skill triggers="stream, asset, stream asset, now" inputs="asset"
# Skill: stream-asset

Stream the asset. Steps:

1. Validate the incoming asset and its parameters.
2. Stream it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=47 label="throttle-data" kind=skill triggers="throttle, data, throttle data, batch" inputs="data"
# Skill: throttle-data

Throttle the data. Steps:

1. Validate the incoming data and its parameters.
2. Throttle it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

@plane z=48 label="retry-asset" kind=skill triggers="retry, asset, retry asset, auto" inputs="asset"
# Skill: retry-asset

Retry the asset. Steps:

1. Validate the incoming asset and its parameters.
2. Retry it using the appropriate backend/tool.
3. Return a structured result the caller can act on.
- cross-check results against [[z=1|cite-sources]].

@plane z=49 label="cache-image" kind=skill triggers="cache, image, cache image, this" inputs="image"
# Skill: cache-image

Cache the image. Steps:

1. Validate the incoming image and its parameters.
2. Cache it using the appropriate backend/tool.
3. Return a structured result the caller can act on.
- cross-check results against [[z=1|cite-sources]].

@plane z=50 label="route-ticket" kind=skill triggers="route, ticket, route ticket, now" inputs="ticket"
# Skill: route-ticket

Route the ticket. Steps:

1. Validate the incoming ticket and its parameters.
2. Route it using the appropriate backend/tool.
3. Return a structured result the caller can act on.

